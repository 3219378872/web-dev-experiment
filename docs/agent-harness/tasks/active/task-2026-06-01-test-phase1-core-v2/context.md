# Context

Phase 1 of test coverage supplementation per spec.
Adds 30 unit tests for cart-service (7), trade-service (18), and item-service (5).
Uses H2 in-memory database with @MockBean Feign clients.

This is the v2 re-submission after PR #28 was prematurely merged and reverted in PR #32.
All CI gates will be strictly followed this time.
