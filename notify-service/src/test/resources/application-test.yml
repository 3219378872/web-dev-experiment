spring:
  cloud:
    bootstrap:
      enabled: false
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
  datasource:
    url: jdbc:h2:mem:notify_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=true
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:sql/schema.sql
  rabbitmq:
    listener:
      simple:
        auto-startup: false
      direct:
        auto-startup: false

mybatis-plus:
  global-config:
    db-config:
      update-strategy: not_null
      id-type: auto
